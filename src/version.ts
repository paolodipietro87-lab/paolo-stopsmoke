/**
 * Impronta della build, iniettata da Vite (vedi `define` in vite.config.ts).
 * Serve a rispondere a una sola domanda: il telefono sta girando il codice di oggi?
 */
export const VERSIONE = __APP_VERSION__;
